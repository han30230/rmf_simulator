# Portable RMF runtime alias used by this simulation snapshot.
# Override at build time when a site maintains its own validated RMF image.
ARG RMF_CORE_SOURCE_IMAGE=ghcr.io/open-rmf/rmf/rmf_demos:jazzy-rmf-latest
FROM ${RMF_CORE_SOURCE_IMAGE}
